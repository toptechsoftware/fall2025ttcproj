# Frontend

This is a minimal React app built with Vite.

Environment:

- VITE_BACKEND_URL: The backend url used by the client (defaults to <http://localhost:4000>)

Run locally:

```powershell
cd frontend
npm install
npm run dev
```
